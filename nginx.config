server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri /index.html;
    }

    # Opcional: servir assets correctamente
    location /assets/ {
        try_files $uri =404;
    }
}
